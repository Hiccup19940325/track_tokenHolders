{
  "name": "monitor",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ testtoken/monitor",
    "create-local": "graph create --node http://localhost:8020/ testtoken/monitor",
    "remove-local": "graph remove --node http://localhost:8020/ testtoken/monitor",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 testtoken/monitor",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
